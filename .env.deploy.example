# === Variables requeridas en el VPS (colocar en $DEPLOY_PATH/.env) ===
# Producción
NODE_ENV=production

# Dominios para CORS (separados por comas). Puedes ajustar en backend si quieres lista fija.
# Si habilitas CORS por origen explícito, incluye ambos dominios:
CORS_ORIGINS=https://tsinit.com,https://www.tsinit.com,https://api.tsinit.com

# Base de datos Postgres (usada por docker-compose.deploy.yml)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGEME
POSTGRES_DB=cms

# URL de conexión Prisma dentro del compose (db es el hostname del servicio)
DATABASE_URL=postgresql://postgres:CHANGEME@db:5432/cms?schema=public

# Puerto backend
PORT=3000

# Secrets JWT
JWT_ACCESS_SECRET=CHANGEME_ACCESS
JWT_REFRESH_SECRET=CHANGEME_REFRESH

# Configuraciones varias
POSTS_CACHE_TTL_MS=30000
# Directorio de uploads dentro del contenedor backend
UPLOADS_DIR=/app/uploads

# API del backend (opcional para SSR):
# API_URL=http://backend:3000/api
# API_BASE=https://api.tu-dominio.com

# === Variables para el workflow de backend ===
# Ruta en el VPS donde se desplegará (debe existir y contener este .env)
# DEPLOY_PATH=/srv/ngcms

# === Variables para el workflow de frontend (hosting compartido) ===
# Configuración de FTP
# Estos van como GitHub Secrets en el repo, no aquí:
# FTP_HOST=files.tsinit.com
# FTP_USERNAME=your-user
# FTP_PASSWORD=your-password
# FTP_REMOTE_DIR=/public_html/
# FTP_PORT=21
