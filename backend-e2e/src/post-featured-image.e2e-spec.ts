// Placeholder: las pruebas reales de featuredImage están en src/posts/
// Se mantiene este archivo porque existía en commits previos y evitará fallos de Jest por suite vacía.
describe('post-featured-image placeholder', () => {
	it('placeholder passes', () => {
		expect(true).toBe(true);
	});
});

