# Copiar a .env y ajustar valores

# === Desarrollo local ===
NODE_ENV=development
SITE_URL=http://localhost:4200
# El compose de dev mapea db en el host 5433 → contenedor 5432
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/cms?schema=public
PORT=3000
POSTS_CACHE_TTL_MS=30000
JWT_ACCESS_SECRET=change_me_access
JWT_REFRESH_SECRET=change_me_refresh

# === Producción (usado por docker-compose.prod.yml) ===
# NODE_ENV=production
# CORS_ORIGINS=https://tu-dominio.com,https://admin.tu-dominio.com
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=cms
# DATABASE_URL=postgresql://postgres:postgres@db:5432/cms?schema=public
# API_URL=http://backend:3000/api
# API_BASE=https://api.tu-dominio.com
# SSR_MICROCACHE=0
